<UserControl x:Class="SlideDock.Views.AppIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:SlideDock.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="50"
             DataContext="{Binding}">
    <!-- Using Border instead of Button to avoid mouse event conflicts with drag and drop -->
    <Border Name="AppIconBorder" 
            PreviewMouseLeftButtonDown="AppIcon_PreviewMouseLeftButtonDown"
            PreviewMouseLeftButtonUp="AppIcon_PreviewMouseLeftButtonUp"
            MouseMove="AppIcon_MouseMove"
            MouseLeave="AppIcon_MouseLeave"
            Background="Transparent"
            ToolTip="{Binding Name}"
            Cursor="Hand"
            Margin="4"
            Padding="6"
            CornerRadius="8">
        <Border.Style>
            <Style TargetType="Border">
                <!-- Enhanced hover effects with smooth transitions and subtle shadow -->
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="Black" BlurRadius="8" Opacity="0.1" ShadowDepth="2"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1.1" Duration="0:0:0.15"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1.1" Duration="0:0:0.15"/>
                                    <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                                     To="0.3" Duration="0:0:0.15"/>
                                    <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                    To="#30FFFFFF" Duration="0:0:0.15"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1" Duration="0:0:0.15"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1" Duration="0:0:0.15"/>
                                    <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                                     To="0.1" Duration="0:0:0.15"/>
                                    <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                    To="Transparent" Duration="0:0:0.15"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Remover App"
                          Command="{Binding RemoveAppCommand}"/>
                <MenuItem Header="Abrir Pasta"
                          Command="{Binding OpenFolderCommand}"/>
            </ContextMenu>
        </Border.ContextMenu>
        <!-- Adding click handling through code-behind instead of Command binding -->
        <StackPanel Orientation="Vertical" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Image Source="{Binding IconSource}"
                   Width="48"
                   Height="48"
                   HorizontalAlignment="Center">
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Source" Value="{Binding IconSource}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IconSource}" Value="{x:Null}">
                                <Setter Property="Source">
                                    <Setter.Value>
                                        <DrawingImage>
                                            <DrawingImage.Drawing>
                                                <GeometryDrawing Brush="LightBlue">
                                                    <GeometryDrawing.Geometry>
                                                        <!-- Updated placeholder size to match new icon size -->
                                                        <RectangleGeometry Rect="0,0,48,48"/>
                                                    </GeometryDrawing.Geometry>
                                                </GeometryDrawing>
                                            </DrawingImage.Drawing>
                                        </DrawingImage>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>

            <!-- Added TextBlock to display file name below icon -->
            <TextBlock Text="{Binding Name}"
                       Foreground="White"
                       FontSize="10"
                       FontWeight="Normal"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="60"
                       Margin="0,4,0,0"
                       HorizontalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="Black" BlurRadius="2" Opacity="0.8" ShadowDepth="1"/>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </Border>
</UserControl>
